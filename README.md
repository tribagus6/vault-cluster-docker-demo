# Vault HA Cluster Demo Using Docker Compose

This repository demonstrates a 3-node HashiCorp Vault Enterprise cluster using Raft storage.

## 🚀 Usage

Clone the repo:

```bash
git clone https://github.com/tribagus6/vault-cluster-docker-demo.git
cd vault-cluster-docker-demo
docker compose up -d

